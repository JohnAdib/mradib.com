import { Inter, Vazirmatn } from "next/font/google";

export const fontFa = Vazirmatn({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-fa",
});

export const fontEn = Inter({
  subsets: ["latin"],
  variable: "--font-en",
});
