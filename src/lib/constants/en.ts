import { profile } from "@/data/profile";

export const separator = " - ";

export const myName = profile.name;
export const myTitle = profile.brand;
export const myJobTitle = profile.jobTitle;

// site title
export const defaultTitle = profile.titleTag;

// site description
export const defaultDescription = profile.shortBio;
